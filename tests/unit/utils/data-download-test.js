import dataDownload from 'dummy/utils/data-download';
import { module, test } from 'qunit';

module('Unit | Utility | data-download', function (hooks) {
  // Replace this with your real tests.
  test('it works', function (assert) {
    let result = dataDownload();
    assert.ok(result);
  });
});
