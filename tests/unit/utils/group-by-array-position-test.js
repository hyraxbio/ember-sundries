import groupByArrayPosition from 'dummy/utils/group-by-array-position';
import { module, test } from 'qunit';

module('Unit | Utility | group-by-array-position', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = groupByArrayPosition();
    assert.ok(result);
  });
});
