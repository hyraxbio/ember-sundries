import scalerArraysMatch from 'dummy/utils/scaler-arrays-match';
import { module, test } from 'qunit';

module('Unit | Utility | scaler-arrays-match', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = scalerArraysMatch();
    assert.ok(result);
  });
});
