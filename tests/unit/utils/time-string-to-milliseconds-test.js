import timeStringToMilliseconds from 'dummy/utils/time-string-to-milliseconds';
import { module, test } from 'qunit';

module('Unit | Utility | time-string-to-milliseconds', function () {
  // TODO: Replace this with your real tests.
  test('it works', function (assert) {
    let result = timeStringToMilliseconds();
    assert.ok(result);
  });
});
