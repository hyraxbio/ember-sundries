import dateFromNow from 'dummy/utils/date-from-now';
import { module, test } from 'qunit';

module('Unit | Utility | date-from-now', function () {
  // TODO: Replace this with your real tests.
  test('it works', function (assert) {
    let result = dateFromNow();
    assert.ok(result);
  });
});
