import objectsInPathCheck from 'dummy/utils/objects-in-path-check';
import { module, test } from 'qunit';

module('Unit | Utility | objects-in-path-check', function (hooks) {
  // Replace this with your real tests.
  test('it works', function (assert) {
    let result = objectsInPathCheck();
    assert.ok(result);
  });
});
