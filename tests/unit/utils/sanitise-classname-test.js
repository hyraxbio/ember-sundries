import sanitiseClassname from 'dummy/utils/sanitise-classname';
import { module, test } from 'qunit';

module('Unit | Utility | sanitise-classname', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = sanitiseClassname();
    assert.ok(result);
  });
});
