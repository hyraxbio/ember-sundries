{{#if this.contentWidth}}
  <SimpleBar style={{this.style}} class={{@wrapperClass}} as |sb|>
    {{yield (hash Content=(component "blank-template"))}}
  </SimpleBar>
{{else}}
  <div class="dummy {{@wrapperClass}}" {{did-insert this.onInsert}}>
    {{yield (hash Content=(component "blank-template"))}}
  </div>
{{/if}}