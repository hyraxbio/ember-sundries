import Component from '@ember/component';
import layout from '../../templates/components/ember-extended-elements/info-popout';

export default Component.extend({
  layout,
  classNames: ['info-popout'],
  classNameBindings: ['classes'],
});